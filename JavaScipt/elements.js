(function(){
	var pElements = documents.getElementsByTagname("p");
	lastElement = pElements[pElements.length];
	for( var i=0; len=pElements.length; i<len; i= i+1){
		alert(pElements[i]);

/var pElements = documents.querrySelector("div p");
alert(pElements.parentNode.tagname);
}()); var pElements = documents.querrySelector("div p");
alert(pElements.length);/


